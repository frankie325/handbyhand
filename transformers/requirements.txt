# ============================================
# 依赖安装说明
# ============================================
# torch 安装（两平台不同）：
#   macOS:  本文件直接装 CPU 版
#   Windows GPU: 先运行 nvidia-smi 确认驱动支持的 CUDA 版本，
#                再：pip install torch --index-url https://download.pytorch.org/whl/cu126
#                之后 pip install -r requirements.txt 不会覆盖 GPU 版（已满足 >=）
# ============================================

torch>=2.10
tensorboard>=2.18
tqdm>=4.67.1
pandas>=2.2
nltk>=3.9
